# Optional Docker-only overrides for local operation.
# Copy to `.env.docker` if you want to keep Docker runtime values separate from `.env.local`.

INGEST_SHARED_SECRET=funding-ops-local-dev-secret
FUNDING_OPS_STORAGE_ROOT=D:/Projects/funding-ops/runtime/data
INGEST_SERVICE_PORT=8787
INGEST_SOURCE_TIMEOUT_MS=20000
INGEST_USER_AGENT=FundingOpsIngest/0.1
SIMPLER_GRANTS_API_KEY=
SAM_API_KEY=
USAJOBS_API_KEY=
USAJOBS_API_EMAIL=
